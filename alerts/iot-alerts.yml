groups:
- name: iot_alerts
  rules:
  - alert: DeviceOffline
    expr: up == 0
    for: 5m
    labels:
      severity: critical
    annotations:
      summary: "Dispositivo {{ $labels.instance }} está offline"
